/*WAP that accepts four numbers. subtract the first number from double the second number. Assign a pointer 
to this result. Print the result using this pointer. Take the third number find the square root of it.
Multiply it with the fourth number. Assign another pointer to another result. Print the pointer using 
this result. */

#include<stdio.h>

int main()
{
    char name[20];
    char country[20];
    int goals;
    int games;

    for(int i = 0; i < 3; i++)
    {
        printf("\nEnter name of player: ");
        scanf("%s", name);
        printf("\nEnter name of country: ");
        scanf("%s", name);
    }
}